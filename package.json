{
  "name": "hof_vs_mixin",
  "version": "1.0.0",
  "description": "Compare the hof and mixin design patterns.",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "benchmark": "^2.1.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": ""
}
